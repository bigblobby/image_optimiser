import React from 'react';
import { connect } from 'react-redux';

class OptimiserOptions extends React.Component {
    render(){
        return (
            <div>Options</div>
        )
    }
}

export default connect(null, null)(OptimiserOptions);
